#!/usr/bin/env python

from setuptools import setup

setup(
        name = 'kerio-api',
        version = '0.1',
        packages = ['kerio'],
        install_requires = [
        ],
        zip_safe = False
)
